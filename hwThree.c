#include <stdio.h>
int main() {
  int x;
  scanf("%d",&x);
  int i=0;
  switch(x) 
	while (i < x)
       for(i=0;i<=x;i++)
       {
			printf(" %d",i);
			continue;
		default: 
			printf("do this first!");
			continue;
		case 3:
			printf("nothing to do!");
			break;
       }
  printf("\n");
  return 0;
}
